<div class="container mx-auto px-4 py-8">
  <div class="max-w-2xl mx-auto">
    <div class="bg-white rounded-lg shadow-md p-6">
      <h1 class="text-2xl font-bold mb-6">Add API Key</h1>
      
      <%= form_with(model: @api_key, url: api_key_path, local: true) do |form| %>
        <% if @api_key.errors.any? %>
          <div class="mb-4 bg-red-50 border border-red-200 rounded-lg p-4">
            <h3 class="font-semibold text-red-900 mb-2">Please fix the following errors:</h3>
            <ul class="list-disc list-inside text-red-700">
              <% @api_key.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>
        
        <div class="mb-6">
          <div class="mb-4">
            <%= form.label :name, class: "block text-sm font-medium text-gray-700 mb-2" %>
            <%= form.text_field :name,
                                placeholder: "e.g., My API Key",
                                class: "w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" %>
          </div>
          
          <div class="mb-4">
            <%= form.label :api_token, "API Key", class: "block text-sm font-medium text-gray-700 mb-2" %>
            <%= form.password_field :api_token,
                                    placeholder: "sk-... (OpenAI) or sk-or-... (OpenRouter)",
                                    class: "w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" %>
            <p class="mt-1 text-sm text-gray-600">
              Your API key will be encrypted and stored securely. We support both OpenAI and OpenRouter API keys.
            </p>
          </div>
        </div>
        
        <div class="flex space-x-4">
          <%= form.submit "Save API Key", 
                          class: "px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700 transition cursor-pointer" %>
          <%= link_to "Cancel", root_path, 
                      class: "px-4 py-2 bg-gray-300 text-gray-700 rounded-md hover:bg-gray-400 transition" %>
        </div>
      <% end %>
    </div>
    
    <div class="mt-6 space-y-4">
      <div class="bg-green-50 border border-green-200 rounded-lg p-4">
        <h3 class="font-semibold text-green-900 mb-2">Option 1: OpenAI API Key</h3>
        <ol class="list-decimal list-inside text-green-800 text-sm space-y-1">
          <li>Go to <a href="https://platform.openai.com/api-keys" target="_blank" class="underline">OpenAI API Keys</a></li>
          <li>Sign in or create an account</li>
          <li>Click "Create new secret key"</li>
          <li>Copy the key (starts with sk-...)</li>
        </ol>
        <p class="mt-2 text-green-800 text-sm">
          <strong>Note:</strong> Requires billing information. Direct access to GPT models.
        </p>
      </div>

      <div class="bg-purple-50 border border-purple-200 rounded-lg p-4">
        <h3 class="font-semibold text-purple-900 mb-2">Option 2: OpenRouter API Key</h3>
        <ol class="list-decimal list-inside text-purple-800 text-sm space-y-1">
          <li>Go to <a href="https://openrouter.ai/keys" target="_blank" class="underline">OpenRouter Keys</a></li>
          <li>Sign in or create an account</li>
          <li>Click "Create Key"</li>
          <li>Copy the key (starts with sk-or-...)</li>
        </ol>
        <p class="mt-2 text-purple-800 text-sm">
          <strong>Benefits:</strong> Access to multiple AI models, flexible pricing, usage tracking.
        </p>
      </div>
    </div>
  </div>
</div>
